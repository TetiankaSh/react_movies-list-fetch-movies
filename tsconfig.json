{
  "extends": "@mate-academy/students-ts-config",
  "include": ["src"],
  "compilerOptions": {
    "sourceMap": false,

    // ✅ Add these so JSX and React types work
    "jsx": "react-jsx",
    "types": ["node", "cypress", "react", "react-dom"]
  }
}
